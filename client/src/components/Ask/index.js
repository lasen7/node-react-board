import React from 'react';
import { Write, MemoList, FloatingButton } from 'components';

const Ask = () => {
  return (
    <div>
      <Write />
      <MemoList />
      <FloatingButton />
    </div>
  );
};

export default Ask;